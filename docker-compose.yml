version: "3.9"

services:
  backend:
    build: .
    container_name: task_tracker_backend
    ports:
      - "8000:8000"
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: task_tracker_redis
    ports:
      - "6379:6379"
